experiment_name: gaussian
random_seed: 1998
head_type: gaussian
dataset:
  type: image
  spec: eva
batch_size: 4
accelerator: gpu
log_dir: results/eva
model_ckpt_path: chkp/eva/gaussian/best_loss.ckpt
